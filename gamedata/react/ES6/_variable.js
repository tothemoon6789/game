// Da hoc qua nhung chua nam vung
const _variables = {
    "var":[
        "Using var outside of a function, it belongs to the global scope",
        "Using var inside of a function, it belongs to that function",
        "Using var inside of a block, like for loop , if ..., the variable belongs to the global scope"
    ],
    "let":[
        "Using let inside of a block, like for loop, if..., the variable is only available INSIDE of that block"
    ],
    "const": [
        "We CAN NOT reassign a constant value",
        "We CAN NOT reassign a constant array",
        "We CAN NOT reassign a constant object",
        "We CAN change the elements of constant array",
        "We CAN change the properties of constant object"
    ]
}